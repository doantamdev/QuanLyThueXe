@model IEnumerable<DoAnCnpm.Models.Role>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/Adminlayout.cshtml";
}

<h2>Quản lý Role</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NameRole)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NameRole)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdRole }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdRole }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdRole })
        </td>
    </tr>
}

</table>
